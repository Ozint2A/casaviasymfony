{% extends 'base.html.twig' %}

{% block title %}TypeAbsence{% endblock %}

{% block body %}
    <h1>TypeAbsence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_absence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_absence.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_absence_index') }}">back to list</a>

    <a href="{{ path('app_type_absence_edit', {'id': type_absence.id}) }}">edit</a>

    {{ include('type_absence/_delete_form.html.twig') }}
{% endblock %}
