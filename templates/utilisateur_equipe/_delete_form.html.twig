<form method="post" action="{{ path('app_utilisateur_equipe_delete', {'id': utilisateur_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur_equipe.id) }}">
    <button class="btn">Delete</button>
</form>
