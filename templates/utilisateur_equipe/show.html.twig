{% extends 'base.html.twig' %}

{% block title %}UtilisateurEquipe{% endblock %}

{% block body %}
    <h1>UtilisateurEquipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur_equipe.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ utilisateur_equipe.jour ? utilisateur_equipe.jour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur_equipe_index') }}">back to list</a>

    <a href="{{ path('app_utilisateur_equipe_edit', {'id': utilisateur_equipe.id}) }}">edit</a>

    {{ include('utilisateur_equipe/_delete_form.html.twig') }}
{% endblock %}
